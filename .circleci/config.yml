workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build
      - confirm_deploy:
          type: approval
          requires:
          - build
      - deploy:
          requires:
          - confirm_deploy
