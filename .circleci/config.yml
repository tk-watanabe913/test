version: 2.1
  python:
    version: 3.5.0
    
test:
  name: run tests
          command: |
            python hello_world_test.py

deployment:
    production:
        branch: master
        commands:
            
